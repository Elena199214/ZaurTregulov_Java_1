package Lesson12andHomework;
/**
 * если нет фигурных скобок между выражениями, тогда, к телу кода относится только первая строчка после выражения.
 * Варианты использования if
 * if(boolean expression)
 * {our code}
 *
 * if(boolean expression)
 *   {our code}
 * else
 *    {our code}
 *
 * if(boolean expression)
 *   {our code}
 *   else if (boolean expression)
 *    {our code}
 *    else
 *    {our code}
 *
 *    else if (boolean expression)
 *    else if (boolean expression)
 *    {our code}
 *    else
 *    {our code}
 *    else
 *    {our code}
 *
 *    == в референс типах данных сравнивает только адреса ссылок. В типах string сравнивают содержимое через equals() в данном типе данных он уже определен
 *    и сравнивает содержимое, при написании своих классов необходимо переопределять метод.
 *
 *    Ternary operator
 *    (boolean expression) ? (if true) : (if false)
 */

public class Options_IF {
}
